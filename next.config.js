/** @type {import('next').NextConfig} */
const nextConfig = {
  images:{
    unoptimized:true,
  },
  optimizeFonts:false,
  output: 'export',
  // Other configurations...
};

module.exports = nextConfig;
